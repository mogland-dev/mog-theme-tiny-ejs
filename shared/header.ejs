<header class="flex items-center justify-between py-10">
  <div>
    <a href="/">
      <div class="flex items-center justify-between">
        <div class="hidden h-6 text-2xl font-semibold sm:block">
          <%= config.seo.title %>
        </div>
      </div>
    </a>
  </div>
  <div class="flex items-center text-base leading-5">
    <div class="hidden sm:block">
      <a class="p-1 font-medium text-gray-900 sm:p-4 dark:text-gray-100" href="/posts">文章</a>

      <a class="p-1 font-medium text-gray-900 sm:p-4 dark:text-gray-100" href="/archives">归档</a>

      <a class="p-1 font-medium text-gray-900 sm:p-4 dark:text-gray-100" href="/friends">友链</a>
      <% for (const page of site.pages) { %>
        <a class="p-1 font-medium text-gray-900 sm:p-4 dark:text-gray-100" href="/<%= page.slug %>">
          <%= page.title %>
        </a>
        <% } %>
    </div>
    <div class="sm:hidden">
      <!-- <button type="button" class="w-8 h-8 ml-1 mr-1 rounded" aria-label="Toggle Menu" onClick={() => {
        setIsOpen(!isOpen);
      }}>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="text-gray-900 dark:text-gray-100"> <path fillRule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clipRule="evenodd"></path></svg>
      </button> -->
      <div class={clsx( isOpen ? "translate-x-0" : "translate-x-full"
        , "fixed w-full h-full top-24 right-0 bg-gray-200 dark:bg-gray-800 opacity-95 z-10 transform ease-in-out duration-300"
        )}>
        <nav class="fixed h-full mt-8">
          <div class="px-12 py-4">
            <a href="/posts" class="text-2xl font-bold tracking-widest text-gray-900 dark:text-gray-100">Posts</a>
          </div>
          <div class="px-12 py-4">
            <a class="text-2xl font-bold tracking-widest text-gray-900 dark:text-gray-100" href="/archives">Archive</a>
          </div>
          <div class="px-12 py-4">
            <a class="text-2xl font-bold tracking-widest text-gray-900 dark:text-gray-100" href="/friends">Friends</a>
          </div>
          <!-- {
            aggregateSnapshot?.aggregatedData.page_meta?.map((item: any) => {
              return (
                <div class="px-12 py-4">
                  <Link href={`/${item.slug}`}>
                    <a key={item.id} class="text-2xl font-bold tracking-widest text-gray-900 dark:text-gray-100">{item.title}</a>
                  </Link>
                </div>
              )
            })
          } -->
        </nav>
      </div>
    </div>
  </div>
</header>